import 'package:flutter/material.dart';

class WantedTab extends StatelessWidget {
  const WantedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wanted'),
      ),
    );
  }
}
