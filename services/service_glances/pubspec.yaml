name: service_glances
description: Glances API client and UI screens for Atrium.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  collection: ^1.18.0
  core_models:
  core_networking:
  core_storage:
  core_ui:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  freezed_annotation: ^3.1.0
  hive_ce: ^2.10.1
  json_annotation: ^4.9.0
  # Pinned exact for F-Droid reproducible builds (same precedent as
  # m3_expressive 1.0.0 in core_ui).
  m3e_design: 0.2.1
  progress_indicator_m3e: ^0.1.1

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5
  json_serializable: ^6.8.0
