name: core_networking
description: >
  Dio factory, dual-URL ConnectionResolver, and standard interceptors
  (auth, retry, logging) for Atrium service modules.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  collection: ^1.18.0
  connectivity_plus: ^7.2.0
  core_models:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
