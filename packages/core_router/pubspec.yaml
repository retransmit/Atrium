name: core_router
description: >
  Route vocabulary and the bottom-nav shell for Atrium. The concrete
  GoRouter is assembled in the app layer.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.3.0

dev_dependencies:
  build_runner: ^2.4.13
