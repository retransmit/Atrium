## 0.1.1+atrium.1
- Forked from upstream `progress_indicator_m3e` 0.1.1 as Atrium's vendored copy.
- fix: offset the linear track start by `+ spec.trackHeight` in
  `lib/src/linear_progress_m3e.dart` (~line 165) so the round stroke caps do
  not eat the visual gap between the active lane and the track.
- chore: conform to the repo's lint rules.

## 0.1.0
- Changelog initialized.
